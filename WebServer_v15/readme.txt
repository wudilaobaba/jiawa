本版本改动：
支持重定向操作 ：
	01代码302，
	02响应头多了Loaction
	03没有响应正文
重定向对应的状态码常用为302，并且要包含一个响应头Loaction,值为要求浏览器再次发起连接时请求的路径，即：希望重定向的路径
重定向的响应中通常不包含响应正文
对此，我们对HttpResponse进行重构，添加重定向功能。

1.在HttpResponse中添加一个重定向方法：
	void sendRedirect(String url)
	在该方法中我们进行重定向相关设置操作
	
2.在注册和登陆的Servlet中，在执行完业务后改为使用重定向方式跳转到提示页面