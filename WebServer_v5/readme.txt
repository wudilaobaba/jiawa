本版本改动：
对v4代码进行优化，将响应内容提成一个对象来表示。复用响应操作代码，并且使得响应内容可以进行设置
1.在http包中定义类HttpResponse,使用该类的每个实例表示一个具体发送给客户端的响应
2.在HttpResponse中定义一个方法：flush,该方法用于将当前响应内容按照HTTP协议的响应格式发送给客户端
3.修改ClientHandler中的响应代码，将以前写死的响应过程改为设置一个HttpResponse对象，最终调用flush给客户端响应即可